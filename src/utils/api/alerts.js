export const error = {
  profileDisabled: "Visiting the profile has failed.",
  imageInvalid: "No valid image uri has been specified."
}

export const success = {
  serverOn: "The server has started."
}

const alerts = {
  error,
  success
};

export default alerts;
